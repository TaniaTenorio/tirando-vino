/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/tirando-vino',
    // output: "export",  // <=== enables static exports
    reactStrictMode: true,
};

export default nextConfig;
